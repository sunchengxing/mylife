<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.recipe.mapper.CategoryMapper">

    <select id="findAllWithCount" resultType="com.recipe.entity.Category">
        SELECT c.id, c.slug, c.name, c.type, c.total_pages as totalPages,
               (SELECT COUNT(*) FROM recipes WHERE category = c.slug) as recipeCount
        FROM categories c
        WHERE c.total_pages > 0
        ORDER BY recipeCount DESC
    </select>

</mapper>
