<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.recipe.mapper.StepMapper">

    <select id="findByRecipeId" resultType="com.recipe.entity.Step">
        SELECT id, recipe_id as recipeId, step_no as stepNo, description, image_url as imageUrl
        FROM steps WHERE recipe_id = #{recipeId}
        ORDER BY step_no
    </select>

</mapper>
